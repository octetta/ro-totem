ro-totem for Windows
====================

Keep ro-totem.exe and ui.html in the same directory, then launch
ro-totem.exe.

The Windows package is preliminary. It requires the Microsoft Edge WebView2
Runtime and a Windows-compatible Skred library.
